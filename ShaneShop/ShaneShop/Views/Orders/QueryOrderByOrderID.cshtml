
@{
    ViewBag.Title = "QueryOrderByOrderID";
}

<h2>QueryOrderByOrderID</h2>

@section scripts
{
    <script>
        angular.module("MainApp", ["ngMaterial", "ngMessages"])
            .controller("MainCtrl", ["$http", function ($http) {
                common.log("angular");
            }]);
    </script>
}

<div ng-module="MainApp" ng-controller="MainCtrl as ctrl" style="padding-top: 50px">
    <br />
    <div class="panel panel-primary">
        <div class="panel-heading">
            <h4 class="panel-title">
                <a data-toggle="collapse" data-parent="#accordion" href="#collapse">查詢訂單</a>
            </h4>
        </div>
        <div id="collapse" class="panel-collapse collapse in">
            <div class="panel-body">

                <form class="form-horizontal" role="form" action="@Url.Action("QueryOrder", "Orders")" method="POST">
                    <div class="row">
                        <div class="col-md-12">
                            <md-input-container class="md-block">
                                <label>訂單編號</label>
                                <input type="text"
                                       class="form-control"
                                       required="required"
                                       name="OrderID"
                                       maxlength="5"
                                       value=""/>
                            </md-input-container>
                        </div>
                        <div class="col-md-12">
                            <button type="submit" class="btn btn-success">
                                查詢
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>